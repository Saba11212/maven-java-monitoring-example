pipeline {
    agent any
    
    stages {
        stage('Checkout Code') {
            steps {
                // Checkout your source code (assuming you have it in a version control system)
                checkout scm
            }
        }

      
    }
}
